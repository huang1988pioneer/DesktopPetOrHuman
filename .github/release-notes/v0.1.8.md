更新內容：
- 12 個角色改用內嵌 SVG 向量圖，五種大小與各種螢幕縮放下都清晰、背景透明。
- 新增 3D 角色與場景 GLB 模型，以及 8 個帶骨架的角色 FBX 模型（素材，尚未用於程式）。
- macOS Apple Silicon 與 Windows x64 self-contained release build。

macOS：下載 ZIP 後解壓縮，執行 DesktopPetOrHuman。
Windows：下載 ZIP 後解壓縮，執行 DesktopPetOrHuman.exe。
